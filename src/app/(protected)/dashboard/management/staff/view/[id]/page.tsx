"use client"
interface ViewStaffProps {
    params: {
        id: string
    }
}
const ViewStaff = ({ params }: ViewStaffProps) => {
    const { id } = params
    return (
        <>
        </>
    )
}

export default ViewStaff